@model User
<div class="card">
    <div class="card-header">
        <h3>Detail User</h3>
    </div>
    <div class="card-body">
        <table class="table">
            <tr>
                <th>Username</th>
                <th>Nama</th>
                <th>Role</th>
                <th>Tanggal Lahir</th>
            </tr>

            <tr>
                <td>@Model.Username</td>
                <td>@Model.Name</td>
                <td>@Model.Role</td>

                <td>
                    @Model.FormatTanggalLahir
                </td>
            </tr>
        </table>
    </div>
</div>